# MDK Examples

A collection of example Mod Developer Kits (MDKs) ranging from simple to complex.

Still a work-in-progress.

Use the `MDK_EXAMPLE_FG7` environment variable to force a specific version of ForgeGradle 7 to use for the example. Mostly used internally to automate testing.
